void main() {
  int x = 1;
  int y = 1;
  scan(x);
  scan(y);
  if (x < y)
    print("x<y");
  if (x > y)
    print("x>y");
  if (x <= y)
    print("x<=y");
  if (x >= y)
    print("x>=y");
  if (x == y)
    print("x==y");
  if (x != y)
    print("x!=y");
}
